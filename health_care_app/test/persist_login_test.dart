// persist_login tests removed because biometric/persistent-login feature
// was removed. Keep no-op test to satisfy CI.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1, equals(1));
  });
}
